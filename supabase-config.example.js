// bird.exe — Supabase configuration
// Paste your values from: Supabase Dashboard → Project Settings → API
// (Project URL + anon/public key — NOT the service_role key!)
//
// After filling this in, save it as workspace/supabase-config.js
// (this example file is committed as documentation; the real one is gitignored)

window.SUPABASE_CONFIG = {
  url: 'https://YOUR-PROJECT.supabase.co',
  anonKey: 'sb_publishable_YOUR_KEY',
};
